% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transferencias.R
\name{get_tc_por_estados}
\alias{get_tc_por_estados}
\alias{get_tc_by_state}
\title{Get constitutional transfers by state}
\usage{
get_tc_por_estados(
  p_estado = NULL,
  p_ano = NULL,
  p_mes = NULL,
  p_transferencia = NULL,
  p_sn_detalhar = NULL,
  use_cache = TRUE,
  verbose = FALSE
)

get_tc_by_state(state_code = NULL, year = NULL, month = NULL,
  transfer_type = NULL, detailed = NULL, use_cache = TRUE, verbose = FALSE)
}
\arguments{
\item{p_estado}{State code(s) from \code{\link[=get_tc_estados]{get_tc_estados()}}. Accepts a vector
or a colon-separated string. Optional.}

\item{p_ano}{Year(s). Accepts a vector or colon-separated string.
Optional.}

\item{p_mes}{Month(s) (1-12). Accepts a vector or colon-separated
string. Optional.}

\item{p_transferencia}{Transfer type code(s) from
\code{\link[=get_tc_transferencias]{get_tc_transferencias()}}. Accepts a vector or colon-separated string.
Optional.}

\item{p_sn_detalhar}{Character. Set to any value to include detailed
breakdown. Omit for summary. Optional.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses an in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL being
called. Useful for debugging or testing in a browser. Defaults to
\code{getOption("tesouror.verbose", FALSE)}.}

\item{state_code}{State code(s) from \code{\link[=get_tc_estados]{get_tc_estados()}}. Accepts a
vector (e.g., \code{c(1, 2)}) or colon-separated string (\code{"1:2"}).
These are \strong{Treasury codes}, not IBGE codes. Optional. Maps to
\code{p_estado}.}

\item{year}{Year(s). Accepts a vector or colon-separated string.
Optional. Maps to \code{p_ano}.}

\item{month}{Month(s) (1-12). Accepts a vector or colon-separated
string. Optional. Maps to \code{p_mes}.}

\item{transfer_type}{Transfer type code(s) from
\code{\link[=get_tc_transferencias]{get_tc_transferencias()}}. Accepts a vector or colon-separated
string. Optional. Maps to \code{p_transferencia}.}

\item{detailed}{Character. Set to any value to include a detailed
breakdown. Omit for summary. Optional. Maps to \code{p_sn_detalhar}.}
}
\value{
A \link[tibble:tibble]{tibble} with transfer data by state.
}
\description{
Retrieves constitutional transfer data aggregated by state.
}
\details{
All codes are \strong{internal Treasury codes} (not IBGE). Use the dictionary
functions to look them up: \code{\link[=get_tc_estados]{get_tc_estados()}} for state codes and
\code{\link[=get_tc_transferencias]{get_tc_transferencias()}} for transfer type codes.

Multi-value parameters accept either a colon-separated string
(\code{"1:2:3"}) or an R vector (\code{c(1, 2, 3)}).

\code{get_tc_by_state()} is an English alias.
}
\examples{
\dontrun{
# Step 1: look up codes (Treasury codes, NOT IBGE)
estados <- get_tc_estados()
tipos   <- get_tc_transferencias()

pe_code <- estados$codigo[estados$nome == "Pernambuco"]

# Step 2: query (pass a vector or colon-separated string)
tc_pe <- get_tc_por_estados(p_estado = pe_code, p_ano = 2023)
tc_multi <- get_tc_por_estados(
  p_estado = c(1, 2), p_ano = 2023, p_mes = c(1, 2)
)
}
}
\seealso{
Other Transferencias: 
\code{\link{get_tc_estados}()},
\code{\link{get_tc_municipios}()},
\code{\link{get_tc_por_estados_detalhe}()},
\code{\link{get_tc_por_municipio}()},
\code{\link{get_tc_por_municipio_detalhe}()},
\code{\link{get_tc_transferencias}()}
}
\concept{Transferencias}
