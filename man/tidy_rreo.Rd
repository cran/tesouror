% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rreo_tidy.R
\name{tidy_rreo}
\alias{tidy_rreo}
\title{Tidy a RREO tibble by topic, reconciling layout drift across years}
\usage{
tidy_rreo(data, topic, regime = NULL)
}
\arguments{
\item{data}{A tibble returned by \code{\link[=get_rreo]{get_rreo()}} or \code{\link[=get_rreo_for_state]{get_rreo_for_state()}},
with at least the columns \code{exercicio}, \code{conta}, \code{coluna}, \code{valor}.}

\item{topic}{Character. Topic name (e.g., \code{"previdencia"}). See
\code{\link[=rreo_layout]{rreo_layout()}} for the supported set.}

\item{regime}{Optional character. Filter to a subset of regimes within
\code{topic} (e.g., \code{"rgps"}, \code{"rpps"}). When \code{NULL} (default) all regimes
are kept.}
}
\value{
A \link[tibble:tibble]{tibble} with the matched rows, plus extra
columns:
\itemize{
\item \code{indicador}: stable indicator name (e.g.,
\code{"resultado_previdenciario_rgps"}).
\item \code{regime}: matched regime.
\item \code{coluna_padrao}, \code{coluna_ano}: see \code{\link[=rreo_normalize_columns]{rreo_normalize_columns()}}.
}
}
\description{
Filters a long RREO tibble (typically produced by \code{\link[=get_rreo]{get_rreo()}}) down to the
rows that match a known indicator for \code{topic} (and optionally \code{regime}),
using the rules in \code{\link[=rreo_layout]{rreo_layout()}}. Account labels are matched on a
year-stable, accent-folded key (Roman numerals and formula text are
stripped before comparison), so the same call returns a coherent series
across years even when SICONFI relabelled the appendix or account.
}
\details{
Currently supported topics:
\itemize{
\item \code{"previdencia"} — federal previdência (RGPS, RPPS civis, FCDF, militares
inativos) for the União sphere. Anexos 04.1 / 04.2 / 04.3 / 04.4 of the
RREO; the layout knows that the RGPS appendix moved from 04.3 (up to 2022) to
04.4 (2023+) and that civis/FCDF moved from 04.1 (up to 2022) to 04.2 (2023+).
}

Pull requests adding new topics to \code{inst/extdata/rreo_layout.csv} are
welcome.
}
\examples{
\dontrun{
library(dplyr)
rreo <- purrr::map_dfr(2019:2023, \(yr) {
  get_rreo(an_exercicio = yr, nr_periodo = 6,
           co_tipo_demonstrativo = "RREO",
           no_anexo = rreo_layout()$no_anexo[
             rreo_layout()$topic == "previdencia" &
             rreo_layout()$regime == "rgps" &
             yr >= rreo_layout()$first_year &
             yr <= rreo_layout()$last_year
           ][1],
           co_esfera = "U", id_ente = 1)
})
tidy_rreo(rreo, topic = "previdencia", regime = "rgps") |>
  filter(coluna_padrao == "DESPESAS LIQUIDADAS ATÉ O BIMESTRE",
         is.na(coluna_ano) | coluna_ano == exercicio) |>
  select(exercicio, indicador, valor)
}
}
\seealso{
Other RREO tidy: 
\code{\link{rreo_layout}()},
\code{\link{rreo_normalize_columns}()}
}
\concept{RREO tidy}
