% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/siconfi.R
\name{get_extrato}
\alias{get_extrato}
\alias{get_delivery_status}
\title{Get delivery status extract}
\usage{
get_extrato(
  id_ente,
  an_referencia,
  use_cache = TRUE,
  verbose = FALSE,
  page_size = NULL,
  max_rows = Inf
)

get_delivery_status(entity_id, year, use_cache = TRUE, verbose = FALSE,
  page_size = NULL, max_rows = Inf)
}
\arguments{
\item{id_ente}{Integer. IBGE code of the entity. \strong{Required}.}

\item{an_referencia}{Integer. Reference year (e.g., \code{2022}). \strong{Required}.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses an in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL being
called. Useful for debugging or testing in a browser. Defaults to
\code{getOption("tesouror.verbose", FALSE)}.}

\item{page_size}{Integer or \code{NULL}. Number of rows per API page.
If \code{NULL} (default), uses the API server default (5000 for
SICONFI/SADIPEM).}

\item{max_rows}{Numeric. Maximum number of rows to return. Defaults
to \code{Inf} (all rows). Useful for quick tests with large datasets
(e.g., \code{max_rows = 100}).}

\item{entity_id}{Integer. IBGE code of the entity. \strong{Required}.
Maps to \code{id_ente}.}

\item{year}{Integer. Reference year (e.g., \code{2022}). \strong{Required}.
Maps to \code{an_referencia}.}
}
\value{
A \link[tibble:tibble]{tibble} with delivery status data including
columns such as \code{exercicio}, \code{cod_ibge}, \code{instituicao}, \code{entregavel},
\code{periodo}, \code{periodicidade}, \code{status_relatorio}, \code{data_status},
\code{forma_envio}, and \code{tipo_relatorio}.
}
\description{
Retrieves the extract of report deliveries for a given entity and reference
year. Useful for checking which reports have been submitted and their status
(approved, rectified, etc.).
}
\details{
\code{get_delivery_status()} is an English alias for \code{get_extrato()}.
}
\examples{
\dontrun{
extrato <- get_extrato(id_ente = 17, an_referencia = 2022)
}
}
\seealso{
Other SICONFI: 
\code{\link{get_anexos}()},
\code{\link{get_dca}()},
\code{\link{get_dca_for_state}()},
\code{\link{get_entes}()},
\code{\link{get_msc_controle}()},
\code{\link{get_msc_orcamentaria}()},
\code{\link{get_msc_patrimonial}()},
\code{\link{get_rgf}()},
\code{\link{get_rgf_for_state}()},
\code{\link{get_rreo}()},
\code{\link{get_rreo_for_state}()}
}
\concept{SICONFI}
