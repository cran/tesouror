% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sadipem.R
\name{get_pvl_tramitacao}
\alias{get_pvl_tramitacao}
\alias{get_pvl_status}
\title{Get PVL processing status (approved non-credit operations)}
\usage{
get_pvl_tramitacao(
  id_pleito,
  use_cache = TRUE,
  verbose = FALSE,
  page_size = NULL,
  max_rows = Inf
)

get_pvl_status(request_id, use_cache = TRUE, verbose = FALSE,
  page_size = NULL, max_rows = Inf)
}
\arguments{
\item{id_pleito}{Integer. Database ID of a \strong{deferred} PVL request
(from the \code{id_pleito} column of \code{\link[=get_pvl]{get_pvl()}}). \strong{Required}.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses an in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL being
called. Useful for debugging or testing in a browser. Defaults to
\code{getOption("tesouror.verbose", FALSE)}.}

\item{page_size}{Integer or \code{NULL}. Number of rows per API page.
If \code{NULL} (default), uses the API server default (5000 for
SICONFI/SADIPEM).}

\item{max_rows}{Numeric. Maximum number of rows to return. Defaults
to \code{Inf} (all rows). Useful for quick tests with large datasets
(e.g., \code{max_rows = 100}).}

\item{request_id}{Integer. Database ID of a \strong{deferred} PVL request.
Obtain from the \code{id_pleito} column of \code{\link[=get_pvl]{get_pvl()}} results, filtered
by \code{status == "Deferido"}. \strong{Required}. Maps to \code{id_pleito}.}
}
\value{
A \link[tibble:tibble]{tibble} with PVL processing status data.
Returns an empty tibble if the PVL is not an approved non-credit
operation.
}
\description{
Retrieves processing status for PVL requests that were \strong{approved
(deferred)} and involve \strong{non-credit operations}. This endpoint only
returns data for PVLs with status \code{"Deferido"}. For most PVLs (e.g.,
archived, in progress, or credit operations), this will return an
empty tibble.
}
\details{
To find PVLs that have data in this endpoint, filter by
\code{status == "Deferido"} in the results of \code{\link[=get_pvl]{get_pvl()}}.

\code{get_pvl_status()} is an English alias.
}
\examples{
\dontrun{
# Step 1: search PVLs and filter for approved ones
pvl_pe <- get_pvl(uf = "PE")
pvl_deferidos <- pvl_pe[pvl_pe$status == "Deferido", ]

# Step 2: pick an id_pleito from the APPROVED requests
id <- pvl_deferidos$id_pleito[1]

# Step 3: get processing status (only works for deferred PVLs)
status <- get_pvl_tramitacao(id_pleito = id)
}
}
\seealso{
Other SADIPEM: 
\code{\link{get_opc_cronograma_liberacoes}()},
\code{\link{get_opc_cronograma_pagamentos}()},
\code{\link{get_opc_taxa_cambio}()},
\code{\link{get_pvl}()},
\code{\link{get_res_cdp}()},
\code{\link{get_res_cronograma_pagamentos}()}
}
\concept{SADIPEM}
