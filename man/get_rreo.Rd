% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/siconfi.R
\name{get_rreo}
\alias{get_rreo}
\alias{get_budget_report}
\title{Get Budget Execution Summary Report data (RREO)}
\usage{
get_rreo(
  an_exercicio,
  nr_periodo,
  co_tipo_demonstrativo,
  no_anexo,
  co_esfera,
  id_ente,
  use_cache = TRUE,
  verbose = FALSE,
  page_size = NULL,
  max_rows = Inf
)

get_budget_report(fiscal_year, period, report_type, appendix,
  sphere, entity_id, use_cache = TRUE, verbose = FALSE,
  page_size = NULL, max_rows = Inf)
}
\arguments{
\item{an_exercicio}{Integer. Fiscal year (e.g., \code{2022}). \strong{Required}.}

\item{nr_periodo}{Integer. Bimester number (1-6). \strong{Required}.}

\item{co_tipo_demonstrativo}{Character. Report type: \code{"RREO"} or
\code{"RREO Simplificado"}. \strong{Required}. Note: \code{"RREO Simplificado"} applies
only to municipalities with fewer than 50,000 inhabitants that opted for
simplified reporting.}

\item{no_anexo}{Character. Appendix name (e.g., \code{"RREO-Anexo 01"}).
\strong{Required}.}

\item{co_esfera}{Character. Government sphere: \code{"M"} (municipalities),
\code{"E"} (states), or \code{"U"} (union). \strong{Required}.}

\item{id_ente}{Integer. IBGE code of the entity. \strong{Required}.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses an in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL being
called. Useful for debugging or testing in a browser. Defaults to
\code{getOption("tesouror.verbose", FALSE)}.}

\item{page_size}{Integer or \code{NULL}. Number of rows per API page.
If \code{NULL} (default), uses the API server default (5000 for
SICONFI/SADIPEM).}

\item{max_rows}{Numeric. Maximum number of rows to return. Defaults
to \code{Inf} (all rows). Useful for quick tests with large datasets
(e.g., \code{max_rows = 100}).}

\item{fiscal_year}{Integer. Fiscal year (e.g., \code{2022}). \strong{Required}.
Maps to \code{an_exercicio}.}

\item{period}{Integer. Bimester number (1-6). \strong{Required}.
Maps to \code{nr_periodo}.}

\item{report_type}{Character. Report type: \code{"RREO"} or
\code{"RREO Simplificado"}. \strong{Required}. Maps to \code{co_tipo_demonstrativo}.}

\item{appendix}{Character. Appendix name (e.g., \code{"RREO-Anexo 01"}).
\strong{Required}. Maps to \code{no_anexo}.}

\item{sphere}{Character. Government sphere: \code{"M"} (municipalities),
\code{"E"} (states), or \code{"U"} (union). \strong{Required}. Maps to \code{co_esfera}.}

\item{entity_id}{Integer. IBGE code of the entity. \strong{Required}.
Maps to \code{id_ente}.}
}
\value{
A \link[tibble:tibble]{tibble} with RREO data including columns such as
\code{exercicio}, \code{demonstrativo}, \code{periodo}, \code{periodicidade}, \code{instituicao},
\code{cod_ibge}, \code{uf}, \code{populacao}, \code{anexo}, \code{rotulo}, \code{coluna}, \code{cod_conta},
\code{conta}, and \code{valor}.
}
\description{
Retrieves data from the Budget Execution Summary Report (RREO) for specific
filtering criteria. The RREO is published bimonthly and contains
information about revenues, expenses, and other budgetary data.
}
\details{
\code{get_budget_report()} is an English-parameter alias for this function.
}
\examples{
\dontrun{
rreo <- get_rreo(
  an_exercicio = 2022, nr_periodo = 6,
  co_tipo_demonstrativo = "RREO",
  no_anexo = "RREO-Anexo 01",
  co_esfera = "E", id_ente = 17
)
}
}
\seealso{
Other SICONFI: 
\code{\link{get_anexos}()},
\code{\link{get_dca}()},
\code{\link{get_dca_for_state}()},
\code{\link{get_entes}()},
\code{\link{get_extrato}()},
\code{\link{get_msc_controle}()},
\code{\link{get_msc_orcamentaria}()},
\code{\link{get_msc_patrimonial}()},
\code{\link{get_rgf}()},
\code{\link{get_rgf_for_state}()},
\code{\link{get_rreo_for_state}()}
}
\concept{SICONFI}
