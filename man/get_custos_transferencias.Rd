% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/custos.R
\name{get_custos_transferencias}
\alias{get_custos_transferencias}
\alias{get_costs_transfers}
\title{Get transfer cost data}
\usage{
get_custos_transferencias(
  ano = NULL,
  mes = NULL,
  natureza_juridica = NULL,
  organizacao_n1 = NULL,
  organizacao_n2 = NULL,
  organizacao_n3 = NULL,
  use_cache = TRUE,
  verbose = FALSE,
  page_size = 500L,
  max_rows = Inf
)

get_costs_transfers(year = NULL, month = NULL,
  legal_nature = NULL, org_level1 = NULL, org_level2 = NULL,
  org_level3 = NULL, use_cache = TRUE, verbose = FALSE,
  page_size = 500, max_rows = Inf)
}
\arguments{
\item{ano}{Integer. Year of the record. Optional.}

\item{mes}{Integer. Month of the record (1-12). Optional.}

\item{natureza_juridica}{Integer. Legal nature of the organization:
\code{1} (Public Company), \code{2} (Public Foundation), \code{3} (Direct
Administration), \code{4} (Autarchy), \code{6} (Mixed Economy Company). Optional.}

\item{organizacao_n1}{Character or integer. SIORG code for the top-level
organization (Ministry level). Use \code{\link[=get_siorg_orgaos]{get_siorg_orgaos()}} to look up
codes. You can pass plain codes (e.g., \code{244}) — they are
automatically zero-padded to the 6-digit format the API expects
(\code{"000244"}). Optional.}

\item{organizacao_n2}{Character or integer. SIORG code for the
second-level organization. Use \code{\link[=get_siorg_estrutura]{get_siorg_estrutura()}} to browse
sub-units. Automatically zero-padded. Optional.}

\item{organizacao_n3}{Character or integer. SIORG code for the
third-level organization. Use \code{\link[=get_siorg_estrutura]{get_siorg_estrutura()}} to browse
sub-units. Automatically zero-padded. Optional.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses an in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL being
called. Useful for debugging or testing in a browser. Defaults to
\code{FALSE}.}

\item{page_size}{Integer. Number of rows per API page. Defaults to
\code{500}. The CUSTOS backend is slow on broad queries: smaller pages
are more robust against HTTP 504 timeouts at the cost of a few
extra round-trips. The server default of 250 is conservative;
1000+ may time out. If the package returns
\code{attr(result, "partial") = TRUE}, lower this further or add a
\code{mes} filter.}

\item{max_rows}{Numeric. Maximum number of rows to return. Defaults
to \code{Inf} (all rows). Useful for quick tests with large datasets
(e.g., \code{max_rows = 100}).}

\item{year}{Integer. Year of the record. Optional. Maps to \code{ano}.}

\item{month}{Integer. Month of the record (1-12). Optional. Maps to
\code{mes}.}

\item{legal_nature}{Integer. Legal nature of the organization:
\code{1} (Public Company), \code{2} (Public Foundation), \code{3} (Direct
Administration), \code{4} (Autarchy), \code{6} (Mixed Economy Company).
Optional. Maps to \code{natureza_juridica}.}

\item{org_level1}{Character or integer. SIORG code for the top-level
organization (Ministry level). Use \code{\link[=get_siorg_organizations]{get_siorg_organizations()}} to
look up codes. Plain codes (e.g., \code{244}) are auto-padded to
\code{"000244"}. Optional. Maps to \code{organizacao_n1}.}

\item{org_level2}{Character or integer. SIORG code (second level).
Use \code{\link[=get_siorg_structure]{get_siorg_structure()}} to browse sub-units. Auto-padded.
Optional. Maps to \code{organizacao_n2}.}

\item{org_level3}{Character or integer. SIORG code (third level).
Use \code{\link[=get_siorg_structure]{get_siorg_structure()}} to browse sub-units. Auto-padded.
Optional. Maps to \code{organizacao_n3}.}
}
\value{
A \link[tibble:tibble]{tibble} with transfer cost data.
}
\description{
Retrieves transfer cost data for federal government organizations.
All parameters are optional filters.
}
\details{
\code{get_costs_transfers()} is an English alias.
}
\examples{
\dontrun{
transf <- get_custos_transferencias(
  ano = 2023, mes = 6,
  organizacao_n1 = 244,  # MEC
  organizacao_n2 = 249   # INEP
)
}
}
\seealso{
Other CUSTOS: 
\code{\link{get_custos_demais}()},
\code{\link{get_custos_depreciacao}()},
\code{\link{get_custos_pensionistas}()},
\code{\link{get_custos_pessoal_ativo}()},
\code{\link{get_custos_pessoal_inativo}()}
}
\concept{CUSTOS}
