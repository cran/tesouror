% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sadipem.R
\name{get_pvl}
\alias{get_pvl}
\alias{get_debt_requests}
\title{Get public debt verification requests (PVL)}
\usage{
get_pvl(
  uf = NULL,
  tipo_interessado = NULL,
  id_ente = NULL,
  use_cache = TRUE,
  verbose = FALSE,
  page_size = NULL,
  max_rows = Inf
)

get_debt_requests(state = NULL, entity_type = NULL,
  entity_id = NULL, use_cache = TRUE, verbose = FALSE,
  page_size = NULL, max_rows = Inf)
}
\arguments{
\item{uf}{Character. State abbreviation (e.g., \code{"PE"}). Optional.}

\item{tipo_interessado}{Character. Type of requesting entity (e.g.,
\code{"Município"}, \code{"Estado"}). Optional.}

\item{id_ente}{Integer. IBGE code of the requesting entity. Optional.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses an in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL being
called. Useful for debugging or testing in a browser. Defaults to
\code{getOption("tesouror.verbose", FALSE)}.}

\item{page_size}{Integer or \code{NULL}. Number of rows per API page.
If \code{NULL} (default), uses the API server default (5000 for
SICONFI/SADIPEM).}

\item{max_rows}{Numeric. Maximum number of rows to return. Defaults
to \code{Inf} (all rows). Useful for quick tests with large datasets
(e.g., \code{max_rows = 100}).}

\item{state}{Character. State abbreviation (e.g., \code{"PE"}). Optional.
Maps to \code{uf}.}

\item{entity_type}{Character. Type of requesting entity (e.g.,
\code{"Município"}, \code{"Estado"}). Optional. Maps to \code{tipo_interessado}.}

\item{entity_id}{Integer. IBGE code of the requesting entity. Optional.
Maps to \code{id_ente}.}
}
\value{
A \link[tibble:tibble]{tibble} with PVL data including \code{id_pleito},
\code{tipo_interessado}, \code{interessado}, \code{cod_ibge}, \code{uf}, \code{status},
\code{tipo_operacao}, \code{finalidade}, \code{credor}, \code{valor}, and more.
}
\description{
Retrieves data from the Public Debt Verification Letters (PVL) system.
These are requests for approval of credit operations and debt by
subnational entities. Use the resulting \code{id_pleito} column to query
detail functions like \code{\link[=get_pvl_tramitacao]{get_pvl_tramitacao()}}, \code{\link[=get_opc_cronograma_liberacoes]{get_opc_cronograma_liberacoes()}},
etc.
}
\details{
\code{get_debt_requests()} is an English alias.
}
\examples{
\dontrun{
# Search all PVLs for Pernambuco
pvl_pe <- get_pvl(uf = "PE")

# Search by IBGE code (Recife)
pvl_recife <- get_pvl(id_ente = 2611606)

# Use id_pleito from results for detail queries:
id <- pvl_pe$id_pleito[1]
pagamentos <- get_opc_cronograma_pagamentos(id_pleito = id)
cdp <- get_res_cdp(id_pleito = id)

# For PVL processing status, filter approved PVLs first:
deferidos <- pvl_pe[pvl_pe$status == "Deferido", ]
if (nrow(deferidos) > 0) {
  status <- get_pvl_tramitacao(id_pleito = deferidos$id_pleito[1])
}
}
}
\seealso{
Other SADIPEM: 
\code{\link{get_opc_cronograma_liberacoes}()},
\code{\link{get_opc_cronograma_pagamentos}()},
\code{\link{get_opc_taxa_cambio}()},
\code{\link{get_pvl_tramitacao}()},
\code{\link{get_res_cdp}()},
\code{\link{get_res_cronograma_pagamentos}()}
}
\concept{SADIPEM}
