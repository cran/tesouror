% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transferencias.R
\name{get_tc_municipios}
\alias{get_tc_municipios}
\alias{get_tc_municipalities}
\title{Get municipality dictionary}
\usage{
get_tc_municipios(
  p_nome = NULL,
  p_uf = NULL,
  use_cache = TRUE,
  verbose = FALSE
)

get_tc_municipalities(name = NULL, state_code = NULL,
  use_cache = TRUE, verbose = FALSE)
}
\arguments{
\item{p_nome}{Character. Partial municipality name for searching (e.g.,
\code{"Recife"}). Optional.}

\item{p_uf}{Numeric or character. State code from \code{\link[=get_tc_estados]{get_tc_estados()}}.
\strong{Not} a state abbreviation or IBGE code. Optional.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses an in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL being
called. Useful for debugging or testing in a browser. Defaults to
\code{getOption("tesouror.verbose", FALSE)}.}

\item{name}{Character. Partial municipality name for searching (e.g.,
\code{"Recife"}). Optional. Maps to \code{p_nome}.}

\item{state_code}{Numeric or character. State code from
\code{\link[=get_tc_estados]{get_tc_estados()}}. \strong{Not} a state abbreviation or IBGE code.
Optional. Maps to \code{p_uf}.}
}
\value{
A \link[tibble:tibble]{tibble} with municipality data including
\code{codigo}, \code{codigo_uf}, and \code{nome}.
}
\description{
Retrieves the dictionary of municipalities and their numeric codes.
Use these codes in the \code{p_municipio} / \code{municipality} parameter of
other functions.
}
\details{
These are \strong{internal Treasury codes}, not IBGE codes.
Filter by state using the numeric code from \code{\link[=get_tc_estados]{get_tc_estados()}}.

\code{get_tc_municipalities()} is an English alias.
}
\examples{
\dontrun{
# Step 1: find the state code (Treasury code, NOT IBGE)
estados <- get_tc_estados()
pe_code <- estados$codigo[estados$nome == "Pernambuco"]

# Step 2: list municipalities for that state
municipios <- get_tc_municipios(p_uf = pe_code)

# Or search by partial name
recife <- get_tc_municipios(p_nome = "Recife")
}
}
\seealso{
Other Transferencias: 
\code{\link{get_tc_estados}()},
\code{\link{get_tc_por_estados}()},
\code{\link{get_tc_por_estados_detalhe}()},
\code{\link{get_tc_por_municipio}()},
\code{\link{get_tc_por_municipio_detalhe}()},
\code{\link{get_tc_transferencias}()}

Other Transferencias dictionaries: 
\code{\link{get_tc_estados}()},
\code{\link{get_tc_transferencias}()}
}
\concept{Transferencias}
\concept{Transferencias dictionaries}
