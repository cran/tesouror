% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/siconfi.R
\name{get_rreo_for_state}
\alias{get_rreo_for_state}
\alias{get_budget_report_for_state}
\title{Get RREO data for all municipalities of a Brazilian state}
\usage{
get_rreo_for_state(
  state_uf,
  an_exercicio,
  nr_periodo,
  co_tipo_demonstrativo,
  no_anexo,
  include_capital = TRUE,
  on_error = c("warn", "stop", "silent"),
  use_cache = TRUE,
  verbose = FALSE,
  page_size = NULL,
  max_rows = Inf
)

get_budget_report_for_state(state_uf, fiscal_year, period,
  report_type, appendix, include_capital = TRUE,
  on_error = c("warn", "stop", "silent"),
  use_cache = TRUE, verbose = FALSE,
  page_size = NULL, max_rows = Inf)
}
\arguments{
\item{state_uf}{Character. Two-letter UF code (e.g., \code{"PE"}, \code{"ES"}).
\strong{Required}.}

\item{an_exercicio}{Integer. Fiscal year. \strong{Required}.}

\item{nr_periodo}{Integer. Bimester (1-6). \strong{Required}.}

\item{co_tipo_demonstrativo}{Character. \code{"RREO"} or \code{"RREO Simplificado"}.
\strong{Required}.}

\item{no_anexo}{Character. Appendix name (e.g., \code{"RREO-Anexo 01"}).
\strong{Required}.}

\item{include_capital}{Logical. Include the state capital? Defaults to \code{TRUE}.}

\item{on_error}{Character. One of \code{"warn"} (default — log and continue),
\code{"stop"} (abort on first failure), or \code{"silent"} (record but no message).}

\item{use_cache}{Logical. If \code{TRUE} (default), uses the in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL for each call.}

\item{page_size}{Integer or \code{NULL}. Rows per API page.}

\item{max_rows}{Numeric. Maximum rows per municipality call.}

\item{fiscal_year}{Integer. Fiscal year. \strong{Required}. Maps to
\code{an_exercicio}.}

\item{period}{Integer. Bimester (1-6). \strong{Required}. Maps to \code{nr_periodo}.}

\item{report_type}{Character. \code{"RREO"} or \code{"RREO Simplificado"}.
\strong{Required}. Maps to \code{co_tipo_demonstrativo}.}

\item{appendix}{Character. Appendix name. \strong{Required}. Maps to \code{no_anexo}.}
}
\value{
A \link[tibble:tibble]{tibble} with RREO rows for all successful
municipalities. If any call failed, has an attribute \code{"failed"} (tibble
with \code{iteration}, \code{id}, \code{error}).
}
\description{
Fetches RREO data for every municipality of \code{state_uf}, looping over
\code{\link[=get_rreo]{get_rreo()}} with fault tolerance: if an individual municipality call fails
after all retries, the failure is recorded and the loop continues. Failed
calls are returned in \code{attr(result, "failed")}.
}
\details{
This is the recommended way to assemble a state-wide panel: it handles
pagination per municipality, uses the cache, and surfaces partial failures
instead of aborting on the first error (see SICONFI behaviour for entities
that have not yet homologated a given report).

\code{get_budget_report_for_state()} is an English-parameter alias.
}
\examples{
\dontrun{
rreo_es <- get_rreo_for_state(
  state_uf = "ES", an_exercicio = 2021, nr_periodo = 6,
  co_tipo_demonstrativo = "RREO", no_anexo = "RREO-Anexo 01"
)
attr(rreo_es, "failed")
}
}
\seealso{
Other SICONFI: 
\code{\link{get_anexos}()},
\code{\link{get_dca}()},
\code{\link{get_dca_for_state}()},
\code{\link{get_entes}()},
\code{\link{get_extrato}()},
\code{\link{get_msc_controle}()},
\code{\link{get_msc_orcamentaria}()},
\code{\link{get_msc_patrimonial}()},
\code{\link{get_rgf}()},
\code{\link{get_rgf_for_state}()},
\code{\link{get_rreo}()}
}
\concept{SICONFI}
