% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/siorg.R
\name{get_siorg_orgaos}
\alias{get_siorg_orgaos}
\alias{get_siorg_organizations}
\title{Get federal organizations from SIORG}
\usage{
get_siorg_orgaos(
  codigo_poder = NULL,
  codigo_esfera = NULL,
  use_cache = TRUE,
  verbose = FALSE
)

get_siorg_organizations(power_code = NULL, sphere_code = NULL,
  use_cache = TRUE, verbose = FALSE)
}
\arguments{
\item{codigo_poder}{Integer. Power branch code: \code{1} = Executive,
\code{2} = Legislative, \code{3} = Judiciary. Optional (default returns all).}

\item{codigo_esfera}{Integer. Government sphere: \code{1} = Federal,
\code{2} = State/District, \code{3} = Municipal. Optional.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses an in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL being
called. Defaults to \code{getOption("tesouror.verbose", FALSE)}.}

\item{power_code}{Integer. Power branch: \code{1} = Executive, \code{2} = Legislative,
\code{3} = Judiciary. Maps to \code{codigo_poder}.}

\item{sphere_code}{Integer. Sphere: \code{1} = Federal. Maps to \code{codigo_esfera}.}
}
\value{
A \link[tibble:tibble]{tibble} with columns including
\code{codigo_unidade} (SIORG code — use as \code{organizacao_n1} in CUSTOS),
\code{codigo_unidade_pai} (parent code — for navigating hierarchy),
\code{nome}, \code{sigla}, \code{codigo_tipo_unidade} (\code{"orgao"} or \code{"entidade"}),
\code{codigo_natureza_juridica}, \code{codigo_esfera}, \code{codigo_poder}.
}
\description{
Retrieves the list of federal government organizations (organs and
entities) from the SIORG system. Use the returned SIORG codes as
\code{organizacao_n1} / \code{org_level1} in CUSTOS API functions like
\code{\link[=get_custos_pessoal_ativo]{get_custos_pessoal_ativo()}}.
}
\details{
\code{get_siorg_organizations()} is an English alias.
}
\examples{
\dontrun{
# List all federal executive branch organizations
orgaos <- get_siorg_orgaos(codigo_poder = 1, codigo_esfera = 1)

# Find a specific ministry by name
mec <- orgaos[grepl("Educação", orgaos$nome), ]
mec_code <- mec$codigo_unidade[mec$sigla == "MEC"]

# Use that code in CUSTOS queries (auto-padded to "000244")
custos <- get_custos_pessoal_ativo(
  ano = 2023, organizacao_n1 = mec_code
)

# Browse children: units whose parent is MEC
mec_filhos <- orgaos[orgaos$codigo_unidade_pai == mec_code, ]
}
}
\seealso{
Other SIORG: 
\code{\link{get_siorg_estrutura}()},
\code{\link{get_siorg_unidade}()}
}
\concept{SIORG}
\concept{SIORG dictionaries}
