% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rreo_tidy.R
\name{rreo_normalize_columns}
\alias{rreo_normalize_columns}
\title{Normalize the \code{coluna} field of a RREO tibble across years}
\usage{
rreo_normalize_columns(data)
}
\arguments{
\item{data}{A tibble returned by \code{\link[=get_rreo]{get_rreo()}} or \code{\link[=get_rreo_for_state]{get_rreo_for_state()}}.
Must contain a \code{coluna} column.}
}
\value{
The input tibble with \code{coluna_padrao} and \code{coluna_ano} appended.
}
\description{
SICONFI's RREO column labels drift over years: the same conceptual column
appears as \code{"DESPESAS LIQUIDADAS ATÉ O BIMESTRE / 2019"} in 2019,
\code{"DESPESAS LIQUIDADAS ATÉ O BIMESTRE"} (no year) in 2021-2022, and
\code{"DESPESAS LIQUIDADAS ATÉ O BIMESTRE / 2023"} in 2023+. This helper adds
two columns:
}
\details{
\itemize{
\item \code{coluna_padrao}: the column label with any trailing \code{"/ YYYY"} or
\code{"EM YYYY"} suffix removed (whitespace squished).
\item \code{coluna_ano}: the year that appeared in the suffix (integer), or \code{NA}
when no year was present. Useful for distinguishing the current-year
column from a comparative previous-year column.
}
}
\examples{
demo <- tibble::tibble(
  coluna = c(
    "DESPESAS LIQUIDADAS ATÉ O BIMESTRE / 2023",
    "DESPESAS LIQUIDADAS ATÉ O BIMESTRE",
    "INSCRITAS EM RESTOS A PAGAR NÃO PROCESSADOS EM 2023"
  )
)
rreo_normalize_columns(demo)
}
\seealso{
Other RREO tidy: 
\code{\link{rreo_layout}()},
\code{\link{tidy_rreo}()}
}
\concept{RREO tidy}
