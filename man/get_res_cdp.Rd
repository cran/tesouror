% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sadipem.R
\name{get_res_cdp}
\alias{get_res_cdp}
\alias{get_debt_capacity}
\title{Get debt capacity result (CDP)}
\usage{
get_res_cdp(
  id_pleito,
  use_cache = TRUE,
  verbose = FALSE,
  page_size = NULL,
  max_rows = Inf
)

get_debt_capacity(request_id, use_cache = TRUE, verbose = FALSE,
  page_size = NULL, max_rows = Inf)
}
\arguments{
\item{id_pleito}{Integer. Database ID from \code{\link[=get_pvl]{get_pvl()}}. \strong{Required}.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses an in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL being
called. Useful for debugging or testing in a browser. Defaults to
\code{getOption("tesouror.verbose", FALSE)}.}

\item{page_size}{Integer or \code{NULL}. Number of rows per API page.
If \code{NULL} (default), uses the API server default (5000 for
SICONFI/SADIPEM).}

\item{max_rows}{Numeric. Maximum number of rows to return. Defaults
to \code{Inf} (all rows). Useful for quick tests with large datasets
(e.g., \code{max_rows = 100}).}

\item{request_id}{Integer. Database ID of the PVL request. Obtain
from the \code{id_pleito} column of \code{\link[=get_pvl]{get_pvl()}} results. \strong{Required}.
Maps to \code{id_pleito}.}
}
\value{
A \link[tibble:tibble]{tibble} with CDP result data.
}
\description{
Retrieves the Debt Capacity Result (Resultado da Capacidade de
Pagamento) linked to a PVL request. The \code{id_pleito} can be obtained
from \code{\link[=get_pvl]{get_pvl()}}.
}
\details{
\code{get_debt_capacity()} is an English alias.
}
\examples{
\dontrun{
cdp <- get_res_cdp(id_pleito = 40353)
}
}
\seealso{
Other SADIPEM: 
\code{\link{get_opc_cronograma_liberacoes}()},
\code{\link{get_opc_cronograma_pagamentos}()},
\code{\link{get_opc_taxa_cambio}()},
\code{\link{get_pvl}()},
\code{\link{get_pvl_tramitacao}()},
\code{\link{get_res_cronograma_pagamentos}()}
}
\concept{SADIPEM}
